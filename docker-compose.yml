services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: rohan
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: devtrack
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: